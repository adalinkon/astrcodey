# Auth token for connecting to the backend HTTP server in web (non-Tauri) mode.
# When set, the frontend includes it as `Authorization: Bearer <token>` on every request.
# In Tauri desktop mode this is not needed — the token is passed at runtime via IPC.
# VITE_AUTH_TOKEN=
